## Impleplementacion de NLTK
```sh
pip install nltk
```
en el shell de python ejecutar los comandos debajo, para obtener recursos para el mudulo NLTK
```sh
    import nltk
    nltk.download('punkt')
    nltk.download('stopwords')
```